<%= form_with(model: wines_oenologist, local: true) do |form| %>
  <% if wines_oenologist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wines_oenologist.errors.count, "error") %> prohibited this wines_oenologist from being saved:</h2>

      <ul>
      <% wines_oenologist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :wine_id %>
    <%= form.text_field :wine_id %>
  </div>

  <div class="field">
    <%= form.label :oenologist_id %>
    <%= form.text_field :oenologist_id %>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.number_field :grade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
